# Studio disabled until homerow mods are stable — saved Studio keymap bindings
# override config/totem.keymap across normal flashes (see zmk.dev/docs/features/studio).
# CONFIG_ZMK_STUDIO=y
# CONFIG_ZMK_STUDIO_LOCKING=n